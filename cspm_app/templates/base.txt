<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSPM MONITOR CORE</title>
    <!-- Fonts: Rajdhani & IBM Plex Mono -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
    
    <!-- Tailwind CSS & Chart.js -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Rajdhani', 'sans-serif'],
                        mono: ['IBM Plex Mono', 'monospace'],
                    },
                    colors: {
                        obsidian: '#050505',
                        panel: '#0B0C10',
                        cyan: {
                            DEFAULT: '#45F3FF',
                            dim: 'rgba(69, 243, 255, 0.1)',
                            glow: 'rgba(69, 243, 255, 0.5)',
                        },
                        amber: {
                            DEFAULT: '#FFB86C',
                        }
                    },
                    backgroundImage: {
                        'grid-pattern': "linear-gradient(to right, #1f2937 1px, transparent 1px), linear-gradient(to bottom, #1f2937 1px, transparent 1px)"
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background-color: #050505;
            color: #e2e8f0;
            background-size: 40px 40px;
            background-image: linear-gradient(to right, rgba(255,255,255,0.03) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(255,255,255,0.03) 1px, transparent 1px);
        }
        .glass-panel {
            background: rgba(11, 12, 16, 0.7);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(69, 243, 255, 0.2);
            box-shadow: 0 0 15px rgba(0,0,0,0.5);
        }
        .nav-link {
            transition: all 0.2s ease;
            border-left: 2px solid transparent;
        }
        .nav-link:hover, .nav-link.active {
            border-left-color: #45F3FF;
            background: linear-gradient(90deg, rgba(69, 243, 255, 0.1) 0%, transparent 100%);
            color: #45F3FF;
            text-shadow: 0 0 8px rgba(69, 243, 255, 0.4);
        }
        .tech-border-b {
            position: relative;
        }
        .tech-border-b::after {
            content: '';
            position: absolute;
            bottom: -1px;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, transparent, #45F3FF, transparent);
        }
    </style>
</head>
<body class="min-h-screen flex flex-col font-sans selection:bg-cyan selection:text-obsidian">

    <!-- Header -->
    <header class="h-16 glass-panel flex items-center justify-between px-8 sticky top-0 z-50">
        <div class="flex items-center gap-3">
            <div class="w-3 h-3 bg-cyan shadow-[0_0_10px_#45F3FF]"></div>
            <h1 class="text-2xl font-bold tracking-widest uppercase">CSPM<span class="text-cyan">Monitor</span></h1>
        </div>
        <nav class="flex gap-8 text-sm font-semibold tracking-wide">
            <a href="{% url 'dashboard' %}" class="nav-link px-4 py-2 uppercase {% if request.resolver_match.url_name == 'dashboard' %}active{% endif %}">Dashboard</a>
            <a href="{% url 'account_list' %}" class="nav-link px-4 py-2 uppercase {% if request.resolver_match.url_name == 'account_list' %}active{% endif %}">Accounts</a>
            <a href="/admin/" class="nav-link px-4 py-2 uppercase opacity-50 hover:opacity-100">Admin</a>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="flex-grow p-8 max-w-7xl mx-auto w-full relative">
        <!-- Decorative corner elements -->
        <div class="fixed top-20 left-4 w-px h-24 bg-gradient-to-b from-cyan to-transparent opacity-30"></div>
        <div class="fixed bottom-4 right-4 w-24 h-px bg-gradient-to-l from-cyan to-transparent opacity-30"></div>
        
        {% block content %}{% endblock %}
    </main>

    <footer class="text-center py-6 text-xs text-slate-600 font-mono">
        SYSTEM STATUS: ONLINE // LOCAL ENVIRONMENT
    </footer>
</body>
</html>