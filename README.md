# CSPM Manager

マルチクラウド環境（AWS, Azure, GCPなど）のアカウント情報と、CSPM製品への接続状況を一元管理するためのDjangoアプリケーションです。

組織構造（部署 > 監視グループ > アカウント）に基づいた階層的な管理と、接続状況の可視化（ダッシュボード）機能を提供します。

![Dashboard Preview](https://via.placeholder.com/800x450.png?text=Dashboard+Screenshot+Here)
<!-- ↑ 後で実際のダッシュボードのスクリーンショットを撮って置き換えるとGoodです -->

## 🚀 主な機能

*   **Operational Dashboard**
    *   管理アカウント総数、接続数などのKPI表示
    *   プロバイダ別（AWS, Azure...）の割合グラフ
    *   月次接続数の推移グラフ（期間指定フィルタ対応）
    *   未接続アカウントのアラートリスト
    *   部署別利用数ランキング
*   **Organization Structure View**
    *   部署 > 監視グループ > アカウント の3階層ツリー表示
    *   アコーディオンUIによる詳細情報の展開
    *   各監視グループの連絡先（責任者, CC, アラート先）管理
*   **Asset Management**
    *   アカウントの新規登録、編集、削除
    *   監視グループIDの管理
    *   キーワードによる横断検索機能
*   **Modern UI**
    *   視認性の高い "Ceramic White" テーマ
    *   TailwindCSS によるレスポンシブデザイン

## 🛠 技術スタック

*   **Backend:** Python 3.x, Django 4.x
*   **Database:** SQLite (Default)
*   **Frontend:** HTML5, TailwindCSS (CDN), Chart.js
*   **Design:** Custom "Ceramic White" Theme

## 📦 インストールと起動手順

### 1. リポジトリのクローン
```bash
git clone https://github.com/あなたのユーザー名/cspm-manager.git
cd cspm-manager
2. 仮想環境の作成と有効化
code
Bash
# Windows
python -m venv venv
venv\Scripts\activate

# Mac/Linux
python3 -m venv venv
source venv/bin/activate
3. パッケージのインストール
code
Bash
pip install -r requirements.txt
4. データベースのセットアップ
code
Bash
python manage.py migrate
5. 管理者ユーザーの作成
code
Bash
python manage.py createsuperuser
6. アプリケーションの起動
code
Bash
python manage.py runserver
ブラウザで http://127.0.0.1:8000/ にアクセスしてください。
📖 使い方
初期データの登録: 画面右上の「+ New Asset」または /admin/ 画面から、部署と監視グループ、アカウントを登録します。
グループIDの付与: 必要に応じて監視グループに識別ID（Group ID）を登録できます。
レポート活用: ダッシュボード右上の期間指定フィルタを使用し、月次報告用の数値を表示できます。
📝 License
This project is intended for internal operational use.
code
Code
---

### 手順 4: GitHubへのプッシュ（参考）

準備ができたら、以下のコマンドでGitHubに上げれば完了です！

```bash
git init
git add .
git commit -m "First commit: CSPM Manager App"
git branch -M main
git remote add origin https://github.com/あなたのユーザー名/リポジトリ名.git
git push -u origin main
